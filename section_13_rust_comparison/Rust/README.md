#Section 13 : Rust

Rust has to be installed to do this.

The Rust server can be compiled and run with :
```
cd get-whole-database/src
./startServer.sh
```
Compilation is intense, Rust really optimizes everything.

Leave the server running and open another terminal
in the above directory to run the comparison script.

