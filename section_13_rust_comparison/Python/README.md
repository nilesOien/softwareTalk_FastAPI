# Section 13 Python

To start the Python server :

* Run installPackages.sh to install the packages
* Run startServer.sh to start the server

That will occupy this terminal, another terminal will have to be
opened to keep going.

The Rust server can be started in the ../Rust directory.

