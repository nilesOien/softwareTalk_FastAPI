# Section 02


In this directory is the program **sunpyPrintVersion.py** and the
subdirectories **uv** and **pip**. 

cd into the subdirectories to install sunpy using either uv or pip
and then run the **sunpyPrintVersion.py** program.

Notice that uv is a lot faster than pip.

uv will be used for the remainder of this discussion.

