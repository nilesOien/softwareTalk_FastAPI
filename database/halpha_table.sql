
CREATE TABLE IF NOT EXISTS halpha (
        url      TEXT NOT NULL,
	datatime TEXT NOT NULL,
	day      TEXT NOT NULL,
	hour     TEXT NOT NULL,
        site     CHAR NOT NULL,
	size     INT  NOT NULL
    );

