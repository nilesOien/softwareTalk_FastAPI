# Section 11.0

The server from section 9.0 must be running for this to work.

Also, R has to be installed, so most people I know will skip this and just look
at the output.

The output is in the daily/ and hourly/ directories. It amounts to plots
of the summaries that the end points that do a database GROUP BY deliver.

The code is in the *.R files if you are interested.
 
